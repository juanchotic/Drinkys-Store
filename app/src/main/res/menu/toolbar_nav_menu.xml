<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/ic_menu_toolbar"
        android:icon="@drawable/ic_menu_toolbar"
        tools:ignore="MenuTitle" />

    <item
        android:id="@+id/ic_search_toolbar"
        android:icon="@drawable/ic_search_toolbar"
        tools:ignore="MenuTitle" />

    <item
        android:id="@+id/ic_car_toolbar"
        android:icon="@drawable/ic_shopping_toolbar"
        tools:ignore="MenuTitle" />
</menu>